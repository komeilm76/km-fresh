import lib from './lib';
const kmFresh = lib.service.fresh;
export const ref = kmFresh.ref;
export const computed = kmFresh.computed;
export const reactive = kmFresh.reactive;
export default kmFresh;
